OPENAI_API_KEY=your_openai_api_key_here

# Supabase project URL, e.g. https://<project-ref>.supabase.co
SUPABASE_URL=
# Supabase service_role key (server-side only — never expose to the frontend).
SUPABASE_KEY=
